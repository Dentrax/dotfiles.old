defaults.pcm.!card 1
defaults.pcm.!device 0
defaults.pcm.card 1
defaults.pcm.device 0
defaults.ctl.card 1

pcm.!default {
    type plug
	slave {
		pcm "hw:1,0"
	}
}

ctl.!default {
	type hw           
	card 1
}
